@model EMMTY_GO_WEB.Models.ReportesCajero

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>ReportesXCajero</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.IdReporte, "NS Reporte", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("IdReporte", String.Empty)
                @Html.ValidationMessageFor(model => model.IdReporte)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.IdCajero, "NS Cajero", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("IdCajero", String.Empty)
                @Html.ValidationMessageFor(model => model.IdCajero)
            </div>
        </div>

        <input type="hidden" id="FromCreateRelationRxC" name="FromCreateRelationRxC" value="false" />

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
